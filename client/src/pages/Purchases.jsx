import React from "react";

export default function Purchases() {
  // in this route we will display the items purchased by the user and showing their status (delivered, order placed ...)
  return (
    <div>
      <h1>Purchases</h1>
      <p className="text-red-500">
        in this route we will display the items purchased by the user and
        showing their status (delivered, order placed ...)
      </p>
    </div>
  );
}
